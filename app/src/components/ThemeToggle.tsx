'use client';

// ThemeToggle is disabled - application is now light-mode only
export function ThemeToggle() {
    // Return null - theme toggle is no longer needed in light-only mode
    return null;
}
